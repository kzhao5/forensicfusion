numpy>=1.23
opencv-python>=4.8
Pillow>=9.5
scikit-image>=0.21
scipy>=1.10
pywavelets>=1.4
tqdm>=4.66
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7

# ML (optional but required for GNN + learned fusion scripts)
torch>=2.1

# For pyIFD; some systems may fail to build jpegio; wrappers handle fallback if missing.
jpegio>=0.2.0; platform_system != "Windows"
